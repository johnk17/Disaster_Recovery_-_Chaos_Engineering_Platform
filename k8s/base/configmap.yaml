apiVersion: v1
kind: ConfigMap
metadata:
  name: my-service-config
  labels:
    app: my-service
    env: production
    version: 1.0.0
data:
  application.properties: |
    # Application Config Overrides
    server.port=8080
