apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata:
  name: dns-failure
  namespace: default
spec:
  action: error
  mode: all
  patterns:
    - '*'
  selector:
    namespaces:
      - default
    labelSelectors:
      'app': 'my-service'
  duration: '1m'
